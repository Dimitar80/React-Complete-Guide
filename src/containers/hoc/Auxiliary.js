// import React from "react";
// We dont need to import React because we dont use any JSX!

const aux = props => props.children;
// props.children are set of adjacent elements in this place

export default aux;
